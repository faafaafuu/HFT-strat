services:
  bot:
    build: .
    container_name: market-heat-signal-bot-dev
    command: watchfiles "python -m app.main" app
    volumes:
      - ./app:/app/app
      - ./config.yaml:/app/config.yaml
      - ./data:/app/data
      - ./logs:/app/logs
      - ./backups:/app/backups
      - ./storage:/app/storage:ro
    env_file:
      - .env
    restart: unless-stopped
